# LinearJ
LinearJ is a simple Java program that can perform basic matrix operations and solve simple DC circuits using modifed nodal analysis.

## Installation
You have two options: (1) Compile to a jar (recommended) or (2) Build from the source

### Option 1: Compiling to jar
```

```